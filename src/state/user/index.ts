import { createSlice } from "@reduxjs/toolkit";

export const users = createSlice({
  name: "user",
  initialState: [],
  reducers: {},
});

// export const { } = user.ac
export default users.reducer;
